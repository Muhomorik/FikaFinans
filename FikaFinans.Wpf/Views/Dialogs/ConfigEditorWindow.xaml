<mah:MetroWindow
    x:Class="FikaFinans.Wpf.Views.Dialogs.ConfigEditorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:FikaFinans.Wpf.ViewModels"
    Title="{Binding WindowTitle}"
    Width="680"
    Height="540"
    MinWidth="480"
    MinHeight="360"
    d:DataContext="{d:DesignInstance Type=viewModels:ConfigEditorViewModel, IsDesignTimeCreatable=True}"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">

    <dxmvvm:Interaction.Behaviors>
        <dxmvvm:CurrentWindowService ClosingCommand="{Binding CancelCommand}" />
    </dxmvvm:Interaction.Behaviors>

    <DockPanel Margin="16">

        <!-- Path label -->
        <TextBlock DockPanel.Dock="Top"
                   Text="{Binding ConfigFilePath}"
                   Style="{StaticResource ff.SmallStyle}"
                   Margin="{StaticResource ff.LabelSpacing}"
                   TextTrimming="CharacterEllipsis"/>

        <!-- Validation warning -->
        <Border DockPanel.Dock="Top"
                Background="{StaticResource ff.WarningBackgroundBrush}"
                BorderBrush="{StaticResource ff.WarningBorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="8,4"
                Margin="{StaticResource ff.CompactSpacing}">
            <Border.Visibility>
                <Binding Path="HasValidationError">
                    <Binding.Converter>
                        <BooleanToVisibilityConverter/>
                    </Binding.Converter>
                </Binding>
            </Border.Visibility>
            <TextBlock Text="{Binding ValidationMessage}"
                       Foreground="{StaticResource ff.WarningForegroundBrush}"
                       Style="{StaticResource ff.SmallStyle}"
                       TextWrapping="Wrap"/>
        </Border>

        <!-- Save / Cancel -->
        <StackPanel DockPanel.Dock="Bottom"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,8,0,0">
            <Button Command="{Binding CancelCommand}"
                    Style="{StaticResource ff.ButtonStyle}"
                    Padding="24,8"
                    Content="Cancel"/>
            <Button Command="{Binding SaveCommand}"
                    Style="{StaticResource ff.ButtonAccentStyle}"
                    Padding="24,8"
                    Margin="8,0,0,0"
                    Content="Save"/>
        </StackPanel>

        <!-- JSON text editor -->
        <TextBox Text="{Binding ConfigText, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 HorizontalScrollBarVisibility="Auto"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12"
                 mah:ControlsHelper.CornerRadius="4"
                 Margin="0,0,0,8"/>
    </DockPanel>
</mah:MetroWindow>
